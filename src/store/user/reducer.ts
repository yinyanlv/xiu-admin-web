const initialState = {
    authorized: false,
    userInfo: null
};

export function userState(state = initialState, action) {

    switch(action.type) {

        default:
            return state;
    }
}
